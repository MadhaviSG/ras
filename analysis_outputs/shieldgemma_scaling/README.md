# shieldgemma_scaling

This directory is **not** a completed ShieldGemma data-scaling ladder.

Present: `probe.json`, `split.json`, `rung_0/` (zero-shot stub, `n_examples: 0`).
Missing: `results.json`, per-rung `metrics.json` / `report.md`, `~/sg_ladder.log`.

The MG-trained ShieldGemma run that actually finished is:

`analysis_outputs/shieldgemma_sft_real/` (`report.md`, `metrics.json`, `run_config.json`, final adapter).
